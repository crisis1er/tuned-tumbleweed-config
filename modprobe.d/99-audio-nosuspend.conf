# Disable HDA Intel audio auto-suspend
# Prevents ~500ms crackle at media playback start
# Replaces tuned [audio] timeout=0 which has a verify bug
options snd_hda_intel power_save=0 power_save_controller=N
